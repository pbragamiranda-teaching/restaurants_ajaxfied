<div class="d-flex justify-content-center mt-4" data-controller="counter">
  <div class="badge badge-primary p-3 mr-3">
    <span data-counter-target="count"><%= @restaurant_count %></span>
  </div>

  <button class="btn btn-outline-secondary" data-action="click->counter#refresh">
        Refresh counter
  </button>
</div>
